const EmptyMessage = () => {
    return (
        <div className="mt-6 text-gray-600 text-lg text-center">
            There are no posts created. Please create the first one.
        </div>
    );
};

export default EmptyMessage;
